// Placeholder marker so the /dropper/active/:id route is planned but not yet
// built in phase 1. Phase 2 replaces this with the real active-tracking screen
// (GPS ping loop + Mark drop + Complete).
export const PHASE_2 = true;
